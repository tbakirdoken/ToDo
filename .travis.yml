language: java
sudo: true
jdk: oraclejdk9
install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:
  - mvn test
addons:
  apt:
    packages:
      - oracle-java9-installer
before_install:
  - chmod +x mvnw