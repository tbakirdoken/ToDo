language: java
sudo: true
dist: trusty
jdk: oraclejdk9
install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:
  - mvn test -B
addons:
  apt:
    packages:
      - oracle-java9-installer